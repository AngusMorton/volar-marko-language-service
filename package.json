{
  "name": "volar-marko-language-service-repository",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel --no-cache"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "typescript": "^5.4.3",
    "turbo": "1.10.2",
    "prettier": "^3.2.5"
  }
}
