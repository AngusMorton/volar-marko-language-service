<!-- type any css in following block to get css completion -->
// <style>.foo { c }</style>

<!-- TS support -->
// <script>
// let foo: string = 123;
// </script>

<!-- custom diagnosis: Only one style tag is allowed. -->
// <style></style>

<my-let />

$ console.log("I'm in a scriptlet");
$ const apple: string = "test";

class {  
  onCreate(input: Input) {
    this.state = {
      ...input,
      something: "else",
    };

    const banana = "string" + apple;
    if (banana === 5) {
      console.log("banana is 6");
    }
  }
}

style {
  /* Latin */
  @font-face {
    font-family: "Roboto";
    font-style: normal;
    font-display: swap;
    font-weight: 300;
    src:
      local("Roboto Light"),
      local("Roboto-Light"),
      url("./roboto-latin-300.woff2") format("woff2");
  }
}

style {
  @font-face {
    font-family: "Roboto";
    font-style: normal;
    font-display: swap;
    font-weight: 300;
    src:
      local("Roboto Light"),
      local("Roboto-Light"),
      url("./roboto-latin-300.woff2") format("woff2");
  }

  @font-face {
    font-family: "Roboto";
    font-style: normal;
    font-display: swap;
    font-weight: 400;
    src:
      local("Roboto"),
      local("Roboto-Regular"),
      url("./roboto-latin-400.woff2") format("woff2");
  }

  @font-face {
    font-family: "Roboto";
    font-style: normal;
    font-display: swap;
    font-weight: 500;
    src:
      local("Roboto Medium"),
      local("Roboto-Medium"),
      url("./roboto-latin-500.woff2") format("woff2");
  }
}